const authValidation = require('./authValidation');
const novelValidation = require('./novelValidation');
const chapterValidation = require('./chapterValidation');
const categoryValidation = require('./categoryValidation');

module.exports = {
  authValidation,
  novelValidation,
  chapterValidation,
  categoryValidation
}; 